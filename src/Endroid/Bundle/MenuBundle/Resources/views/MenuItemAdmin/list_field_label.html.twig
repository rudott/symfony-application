{% extends admin.getTemplate('base_list_field') %}

{% block field %}

    {% set route = route(object.routeKey) %}

    {% if route %}

        {{ route.label }}

    {% else %}

        {{ object }}

    {% endif %}

{% endblock field %}
